class Solution:
    def shuffle(self, nums: List[int], n: int):
        l=[]
        x=nums[0:n]
        y=nums[n:]
        for i in range(n):
            l.append(x[i])
            l.append(y[i])
        return l
